---
"@velum-labs/routekit": patch
---

Validate all remote qualification models with one catalog request so wide eval plans reach their advertised execution timeout instead of spending roughly two minutes on serial SSH preflight calls.
