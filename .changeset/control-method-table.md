---
"@velum-labs/routekit-control": minor
---

Declare the daemon control protocol as a single method table and bind remaining daemon use cases through owned application services so parameter contracts, authorization, mutation classification, idempotency, and product telemetry are no longer restated by the dispatcher or the product client.
