---
"@velum-labs/routekit-daemon": patch
---

Bind token and telemetry control methods through application services so every daemon use case owns its handler group the same way lifecycle, doctor, and launcher already do.
