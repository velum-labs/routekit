---
"@velum-labs/routekit": minor
"@velum-labs/routekit-eval-contracts": minor
"@velum-labs/routekit-eval-service": minor
---

Replace the placeholder JSON-spec evaluator with an Effect-native eval application service that runs `*.eval.ts` workloads through the Ori-compatible author subset, normalizes candidate and judge evidence, stores immutable local run documents, and exposes the path/workload flow through the CLI.
