---
"@velum-labs/routekit": patch
---

Use a provider-safe configurable timeout for eval qualification, fail unsuccessful runs with actionable non-zero CLI errors, and retain calls observed before interruption in the failed-run ledger.
