# @velum-labs/routekit-registry

Product-neutral generated provider, subscription, catalog, capability, pricing,
and local model metadata from `spec/registry`.

Fusion identities and panel presets intentionally live in
`@fusionkit/registry`, which may depend on this package while RouteKit packages
must never depend on FusionKit.
