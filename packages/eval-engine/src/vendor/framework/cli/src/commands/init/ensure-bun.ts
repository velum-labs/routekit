/**
 * Previously installed the bun CLI for `ori init`. This product's runtime is
 * Node 22.22+; workspace installs go through npm. The file remains so the
 * extracted provenance map still has its destination.
 */
export {};
