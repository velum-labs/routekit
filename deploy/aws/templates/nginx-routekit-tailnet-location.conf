# Keep long-running and streaming RouteKit requests unbuffered and allow
# non-streaming model calls enough time to produce response headers.
proxy_http_version 1.1;
proxy_buffering off;
proxy_request_buffering off;
proxy_read_timeout 600s;
proxy_send_timeout 600s;
send_timeout 600s;
