# RouteKit real-account qualification — 2026-07-22

Issue: [ENG-679](https://linear.app/velum-labs/issue/ENG-679/run-and-record-the-routekit-real-account-matrix)

Result: **3 Pass / 4 Fail**

Launch consequence: L06 remains open; the four Fail rows must be rerun on hosts
with the required enrolled accounts and authenticated clients.

## Tested revision and environment

- RouteKit: `@velum-labs/routekit` 0.8.0
- Git revision:
  [`dad16c53c0e083a51d41df59149a21964d27cc12`](https://github.com/velum-labs/routekit/commit/dad16c53c0e083a51d41df59149a21964d27cc12)
- Evidence date: 2026-07-22 UTC
- Runtime: Linux x64, Node.js 22.22.2
- Working tree during the recorded runs: clean
- Live client-to-RouteKit gateway requests observed: 3
- Authorized aggregate gateway-request maximum: 8
- Provider-internal retry requests and invoice charges: not measured

The matrix ran each route separately with `ROUTEKIT_LIVE_E2E=1`,
`--route <stable-anchor>`, and its descriptor's minimum hard request budget.
Each invocation first ran the deterministic protocol checks. Raw prompts,
responses, PTY transcripts, credentials, account identifiers, home paths, and
authorization headers are intentionally not durable evidence.

The tested matrix implementation is
[`scripts/routekit-e2e-matrix.mjs`](https://github.com/velum-labs/routekit/blob/dad16c53c0e083a51d41df59149a21964d27cc12/scripts/routekit-e2e-matrix.mjs);
the seven-route allowlist and report schema are
[`scripts/routekit-qualification.mjs`](https://github.com/velum-labs/routekit/blob/dad16c53c0e083a51d41df59149a21964d27cc12/scripts/routekit-qualification.mjs).

## Result matrix

| Stable route | Account mode | Client / provider version | Live model and protocol | Tools / stream / reasoning | Cancellation / failure / fallback | Billing evidence | Setup / restore | Result |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `route-openai-api` | User-owned `OPENAI_API_KEY` | RouteKit 0.8.0; provider API revision not advertised | `openai/gpt-5.5`; OpenAI Chat `/v1/chat/completions` | Pass / Pass / Pass | Pass / Pass / no RouteKit fallback | One successful request observed through the isolated RouteKit counting proxy; direct `api.openai.com` route; OpenAI API account owns billing | Not applicable / not applicable | **Pass** |
| `route-anthropic-api` | User-owned `ANTHROPIC_API_KEY` | RouteKit 0.8.0; Anthropic `2023-06-01` | `anthropic/claude-sonnet-4-6`; Messages `/v1/messages` | Pass / Pass / Pass | Pass / Pass / no RouteKit fallback | One successful request observed; direct `api.anthropic.com` route; Anthropic API account owns billing | Not applicable / not applicable | **Pass** |
| `route-openrouter-api` | User-owned `OPENROUTER_API_KEY` | RouteKit 0.8.0; provider API revision not advertised | `openrouter/openai/gpt-4o-mini`; OpenAI Chat `/v1/chat/completions` | Pass / Pass / Pass | Pass / Pass / no RouteKit fallback | One successful request observed to the OpenRouter route; OpenRouter credits own billing and OpenRouter retains provider-managed upstream routing | Not applicable / not applicable | **Pass** |
| `route-codex-subscription` | Enrolled Codex subscription OAuth | Codex client unavailable; no enrolled account | Responses `/v1/responses`; no live model discovered | Pass / Fail / Pass in deterministic/live order | Pass / Pass / no API-key fallback | Zero gateway requests; no subscription usage observed | Fail / Fail | **Fail — `account-unavailable`** |
| `route-claude-code-subscription` | Enrolled Claude Code subscription OAuth | Claude client unavailable; no enrolled account | Messages `/v1/messages`; no live model discovered | Pass / Fail / Pass in deterministic/live order | Pass / Pass / no API-key fallback | Zero gateway requests; no subscription usage observed | Fail / Fail | **Fail — `account-unavailable`** |
| `route-cursor-ide` | Authenticated Cursor desktop plus selected RouteKit route | Cursor desktop unavailable on this Linux worker | Custom OpenAI `/v1/cursor/chat/completions`; no live model call | Fail / Fail / Fail | Deterministic cancellation Pass; desktop failure/fallback unverified | Zero gateway requests | Fail / Fail | **Fail — `manual-evidence-unavailable`** |
| `route-cursor-agent` | Authenticated `cursor-agent` plus selected RouteKit route | `cursor-agent` unavailable | Custom OpenAI `/v1/cursor/chat/completions`; no live model call | Pass / Fail / Pass in deterministic/live order | Pass / Pass / no RouteKit fallback | Zero gateway requests | Fail / Fail | **Fail — `client-unavailable`** |

For the three API rows, tools and reasoning are deterministic wire-compatibility
checks and streaming is additionally exercised by the real-account request.
The successful namespaced request, selected credential mode, direct provider
configuration, and zero-cross-provider deterministic assertion form the
billing/fallback attribution. This report does not claim independent access to
provider invoice dashboards.

## Shared deterministic evidence

The following passed at the exact revision above:

- client disconnect cancels the upstream streaming response body;
- tool-call and reasoning frames survive each selected protocol dialect;
- provider-native failure reaches the caller;
- every non-selected provider receives zero calls after the selected provider
  fails;
- the gateway-request budget rejects an over-budget route selection before live
  traffic;
- the committed report was independently scanned for credential, authorization,
  prompt, response, and local-path material;
- all seven stable anchors are unique and completeness detects missing or
  duplicate results.

The focused schema suite passed 5/5. The full seven-route credential-free run
passed 19 checks with one expected local `cursor-agent` availability Skip; live
qualification converts that availability result to the explicit Fail shown
above.

## Fail register and rerun criteria

### Codex subscription

Rerun after enrolling a real Codex account in an isolated RouteKit profile and
installing the exact official Codex client to record its version. Exercise
login/import, a live Responses turn, quota/failure attribution, removal, and
exact restoration. No OpenAI API-key request may occur during exhaustion.

### Claude Code subscription

Rerun after enrolling a real Claude Code account and installing the exact
Claude client. Exercise login/import, a live native Messages turn,
quota/failure attribution, managed removal, and exact restoration. No Anthropic
API-key request may occur during exhaustion.

### Cursor IDE

Rerun `routekit cursor --ide` on a supported desktop host with an authenticated
Cursor build. Record the exact build, isolated custom-endpoint setup, tools,
streaming, reasoning degradation, cancellation, failure attribution, observed
provider request count, and restoration. This Linux worker has no Cursor
desktop and Cursorkit's desktop launcher is macOS-oriented, so no GUI artifact
could be produced here.

### `cursor-agent`

Rerun with an installed, authenticated `cursor-agent`. Record its exact
version, one custom-endpoint tool turn, streaming, reasoning degradation,
cancellation, failure attribution, session behavior, and exact teardown or
restore.

## Sanitization review

The durable report contains only stable route IDs, public package/client/model
versions, public provider hosts, fixed capability outcomes, fixed reason codes,
request counts, and source links. It contains no credential values, OAuth
material, account names, account IDs, prompts, responses, local home paths,
authorization headers, or raw transcripts.
