# RouteKit

Configure, serve, and use model gateways for coding agents.

```bash
pnpm install --frozen-lockfile
pnpm check
pnpm build
pnpm test
```

The published CLI is `@velum-labs/routekit` (`routekit` on your `PATH`).

See `docs/` for routes, billing, configuration, and qualification evidence.
